{
  "name": "planner-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "build": "npm run build -w @acme/web",
    "dev": "npm run dev -w @acme/web",
    "lint": "npm run lint -w @acme/web",
    "prepare": "husky install || true"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "husky": "^9.0.0",
    "jest": "^30.1.3",
    "ts-jest": "^29.4.1"
  }
}
